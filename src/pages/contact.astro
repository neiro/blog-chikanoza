---
import Layout from '~/layouts/PageLayout.astro';
import HeroText from '~/components/widgets/HeroText.astro';
import ContactUs from '~/components/widgets/Contact.astro';
import Features2 from '~/components/widgets/Features2.astro';
import { getDictionary, getLocaleFromUrl } from '~/i18n';

const locale = getLocaleFromUrl(Astro.url);
const dict = getDictionary(locale);

const metadata = {
  title: dict.contactPage.metadataTitle,
};
---

<Layout metadata={metadata}>
  <!-- HeroText Widget ******************* -->

  <HeroText tagline={dict.contactPage.tagline} title={dict.contactPage.title} />

  <ContactUs
    id="form"
    title={dict.contactPage.formTitle}
    subtitle={dict.contactPage.formSubtitle}
    inputs={[
      {
        type: 'text',
        name: 'name',
        label: dict.contactPage.nameLabel,
        required: true,
      },
      {
        type: 'email',
        name: 'email',
        label: dict.contactPage.emailLabel,
        autocomplete: 'email',
        required: true,
      },
    ]}
    textarea={{
      label: dict.contactPage.messageLabel,
      required: true,
    }}
    disclaimer={{
      label: dict.contactPage.disclaimer,
      required: true,
    }}
    description={dict.contactPage.description}
    action="mailto:viacheslav.redkin@chikanoza.com"
  />

  <!-- Features2 Widget ************** -->

  <Features2 title={dict.contactPage.helpTitle} items={dict.contactPage.helpItems} />
</Layout>
